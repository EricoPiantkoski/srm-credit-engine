---
description: Reconexão de Arquitetura (Lê todo o repositório de diretrizes corporativas AGENTS.md, .specs, .agents)
---
1. Leia `./.agents/AGENTS.md` com a ferramenta `read`.
2. Para cada diretório em `./.specs` e `./.agents`, use `glob` com o padrão `**/*` passando o diretório como `path`; leia com `read` todos os arquivos `.md` encontrados, incluindo subpastas.
3. Após absorver as regras, confirme em uma frase curta que todas as diretrizes foram absorvidas e serão seguidas rigorosamente.