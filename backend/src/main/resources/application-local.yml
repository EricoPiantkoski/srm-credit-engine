app:
  cors:
    allowed-origins: http://localhost:5173
  security:
    expose-docs: true

spring:
  datasource:
    url: jdbc:postgresql://localhost:5656/srm_credit
    username: postgres
    password: postgres